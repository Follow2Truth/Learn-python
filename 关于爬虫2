爬取一个网页的步骤
1. 打开要爬取的网页。2. 打开开发者工具，并让请求重新发一次（刷新一下，简单讲就是抓包）。 3.找到正确的请求。4. 用程序模拟发送。
然后关于利用正则表达式来抓取自己想要的内容
关于regular expression, 使用re module
import re
print(re.split(r'\s*','Here are we'))
()用来组团，其他还有大量的规则。
re.findall('左边的字符串(.*?)右边的字符串',等待匹配的字符串,flags)
