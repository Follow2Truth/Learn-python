迭代器对象可以在for循环中使用，其好处是不需要对下标进行迭代，但在某些情况下，我们既希望获得下标，又希望获得对应的值，可以将
迭代器传给enumerate函数，每次迭代都会返回一组（index，value）组成的元组。

迭代器必须实现_iter_方法，_iter_()返回的对象支持next方法，返回迭代器中的下一个元素。

字典对象的iterkeys,itervalues,iteritems方法返回的都是迭代器，许多标准库函数返回的也是迭代器。

还可以自定义迭代器。

生成器使用 yield 关键字将值输出，而迭代器则通过 next 的 return 将值返回；与迭代器不同的是，
生成器会自动记录当前的状态，而迭代器则需要进行额外的操作来记录当前的状态。
